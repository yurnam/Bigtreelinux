# Build configuration for BigTreeTech PandaTouch – minimal + framebuffer
#
# Produces a Linux image with framebuffer LCD support and BusyBox.
# Used by build-minimal.sh.
#
# For serial-console-only (no framebuffer): see pandatouch-minimal.conf
# For the full image (WiFi, touch, USB tools): see pandatouch.conf

BUILDROOT_CONFIG=esp32s3_pandatouch_fb
