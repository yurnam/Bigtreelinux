# Build configuration for BigTreeTech PandaTouch – minimal / quick-start
#
# Produces a minimal Linux image (BusyBox + serial console only).
# Suitable for first-time bring-up and kernel testing.
#
# For the full-featured build (WiFi, framebuffer, touch, USB, tools):
#   see pandatouch.conf

BUILDROOT_CONFIG=esp32s3_pandatouch_minimal
